1. As linguagens interpretadas são traduzidas em tempo de execução. Enquanto as compiladas gerarão um codigo-fonte no qual será executado por uma maquina virtual (ou real) no momento em que for chamado.

2. Erros de sintaxe, erro logico e erro de execução

Um erro de sintaxe é quando esquecemos algo que é regra da linguagem, erro logico é quando não temos o resultado que pensamos que iria ter e erro de execução é um erro causado por um fator externo.

3. Os principais tipos de dados de uma linguagem de programação são: int, float, double, string e boolean.

	Exemplos:

		int x = 10;
		float y = 3.14;
		double z = 10.000.000;
		string = "casa";
		boolean = false;

4. Podemos dividir nosso codigo por funções, no qual podemos passar parametros e dentro dela realizar ações que queiramos, e a vantagem disso é que sempre que precisarmos dessa função podemos chama-la sem ter que reescrever tudo novamente.

5. No exemplo podemos importar o modulo inteiro por:

	import calculadora

Se quisermos importar apenas uma função (como somar) podemos fazer:
	
	import.somar  

Se quisermos importar e renomear uma função podemos fazer: 

	import.multiplicar as mult

